---
description: Phase 0 spike — verify the plugin is installed and slash commands work.
argument-hint: ""
allowed-tools:
  - "Bash(echo:*)"
---

Run a plain echo to confirm the Tesserae plugin's slash command pipeline is wired up correctly.

!`echo "tesserae-plugin-spike-OK $(date -u +%FT%TZ)"`
